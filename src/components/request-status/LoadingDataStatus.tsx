import LoadingSpinner from '../ui/loading-spinner/LoadingSpinner';

const LoadingDataStatus = () => {
	return (
		<div className='container-large'>
			<LoadingSpinner />
		</div>
	);
};

export default LoadingDataStatus;
